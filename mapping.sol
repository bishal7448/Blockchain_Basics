// SPDX-License-Identifier: MIT

pragma solidity >= 0.7.0 < 0.9.0;

struct School {
    string name;
    uint class;
    string sec;
    uint roll;
}
…        school[id] = School(name, class, sec, roll);
    }
    
}
